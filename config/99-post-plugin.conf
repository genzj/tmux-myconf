# vim: foldmethod=marker:foldenable ft=tmux

### Post-plugin configurations {{{

#### Theme configurations {{{

# Catppuccin's default mode style isn't very readable on some VMs
set -gF mode-style "fg=#{@thm_bg},bg=#{@thm_blue},bold"

# }}}

# }}}

